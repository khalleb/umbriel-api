export enum UserTypes {
  ADMIN = 'admin',
  CLIENT = 'client',
}

export enum Routes {
  USER = 'user',
  SESSION = 'session',
  TAG = 'tag',
  CONTACT = 'contact',
  SENDER = 'sender',
  TEMPLATE = 'template',
  MESSAGE = 'message',
}

export enum RoutesType {
  STORE = 'store',
  UPDATE = 'update',
  SHOW = 'show',
  DELETE = 'delete',
  INACTIVATE_ACTIVATE = 'inactivate-activate',
  INDEX = 'index',
  DELETE_TAG = 'delete-tag',
  INSCRIBE_DESCRIBE = 'inscribe-describe',
  IMPORT = 'import',
  SEND = 'send',
  GET_BY_NAME_LIKE = 'get-by-name-like',
  UPDATE_PASSWORD = 'update-password',
}
