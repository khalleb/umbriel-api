export enum RoutesType {
  STORE = 'store',
  UPDATE = 'update',
  SHOW = 'show',
  DELETE = 'delete',
  INACTIVATE_ACTIVATE = 'inactivate-activate',
  INDEX = 'index',
}

export enum UserTypes {
  ADMIN = 'admin',
  CLIENT = 'client',
}

export enum Routes {
  USER = 'user',
  SESSION = 'session',
  TAG = 'tag',
  CONTACT = 'contact',
  SENDER = 'sender',
  TEMPLATE = 'template',
  MESSAGE = 'message',
}
